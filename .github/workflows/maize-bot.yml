name: Maize Market Bot

on:
  schedule:
    # Runs at 2:30 AM & 2:30 PM UTC = 8:00 AM & 8:00 PM IST
    - cron: '30 2,14 * * *'
  
  workflow_dispatch:  # Allows manual trigger from GitHub UI

jobs:
  run-maize-bot:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4  # Updated to v4
      
      - name: Set up Python
        uses: actions/setup-python@v5  # Updated to v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Run maize automation
        env:
          GREEN_API_INSTANCE: ${{ secrets.GREEN_API_INSTANCE }}
          GREEN_API_TOKEN: ${{ secrets.GREEN_API_TOKEN }}
          WHATSAPP_PHONE: ${{ secrets.WHATSAPP_PHONE }}
          PERPLEXITY_KEY: ${{ secrets.PERPLEXITY_KEY }}
          MONGODB_URI: ${{ secrets.MONGODB_URI }}
        run: python maize_automation.py
      
      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4  # Updated to v4 (fixes the error!)
        with:
          name: execution-logs-${{ github.run_number }}  # Added unique name
          path: |
            *.log
          retention-days: 7  # Keep logs for 7 days
